---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-loki-backend-0
  labels:
    volume: data
    name: data
    type: longhorn
spec:
  capacity:
    storage: 12Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn-data
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeAttributes:
      numberOfReplicates: "1"
      staleReplicaTimeout: "2880"
      dataLocality: "strict-local"
    volumeHandle: pvc-loki-backend # must match volume name from Longhorn

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-loki-write-0
  labels:
    volume: data
    name: data
    type: longhorn
spec:
  capacity:
    storage: 12Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn-data
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeAttributes:
      numberOfReplicates: "1"
      staleReplicaTimeout: "2880"
      dataLocality: "strict-local"
    volumeHandle: pvc-loki-write # must match volume name from Longhorn
