---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mktxp-exporter-servicemonitor
  namespace: monitoring
  labels:
    app: mktxp-exporter
spec:
  endpoints:
    - port: metrics
      interval: 30s
  selector:
    matchLabels:
      app: mktxp-exporter
