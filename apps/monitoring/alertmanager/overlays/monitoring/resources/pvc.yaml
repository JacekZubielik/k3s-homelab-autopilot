---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storage-alertmanager-0
  namespace: monitoring
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn-data
  resources:
    requests:
      storage: 100Mi
  # volumeMode: Filesystem
  volumeName: pv-alertmanager-0
  # selector:
  #   matchLabels:
  #     volume: storage