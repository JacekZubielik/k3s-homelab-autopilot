---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-esphome-data
  namespace: iot
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  volumeName: pvc-esphome-data # pv-esphome-data
  storageClassName: longhorn-data
