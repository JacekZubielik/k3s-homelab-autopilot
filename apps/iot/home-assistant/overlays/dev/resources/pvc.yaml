---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-ha-data
  namespace: iot
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: longhorn-data
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  volumeName: pv-ha-data
  selector:
    matchLabels:
      volume: ha-data