---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: homeassistant-test
  labels:
    app: homeassistant-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: homeassistant-test
  template:
    metadata:
      labels:
        app: homeassistant-test
        name: homeassistant-test
    spec:
      hostNetwork: false
      containers:
      - name: homeassistant-test
        image: homeassistant/home-assistant:2023.10
        imagePullPolicy: IfNotPresent
        ports:
        - name: web
          containerPort: 8123
        env:
        - name: TZ
          valueFrom:
            configMapKeyRef:
              name: home-assistant-timezone
              key: TZ
        resources:
          requests:
            cpu: 100m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 2Gi
        volumeMounts:
        - name: homeassistant-data-volume
          mountPath: /config
      volumes:
      - name: homeassistant-data-volume
        persistentVolumeClaim:
          claimName: pvc-homeassistant-data
