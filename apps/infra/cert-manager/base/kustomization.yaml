---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: cert-manager
metadata:
  labels:
    app.kubernetes.io/name: cert-manager
  name: cert-manager

configMapGenerator:
- name: cert-manager-tz
  literals:
  - TZ=CET
