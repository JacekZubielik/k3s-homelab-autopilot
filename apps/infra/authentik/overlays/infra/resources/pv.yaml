---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-authentik-psql-0
  labels:
    volume: data
    name: data
    type: longhorn
spec:
  capacity:
    storage: 8Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn-data
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeAttributes:
      numberOfReplicates: "1"
      staleReplicaTimeout: "2880"
      dataLocality: "strict-local"
    volumeHandle: pvc-authentik-psql # must match volume name from Longhorn

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-authentik-redis-0
  labels:
    volume: redis-data
    name: redis-data
    type: longhorn
spec:
  capacity:
    storage: 8Gi
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: longhorn-data
  csi:
    driver: driver.longhorn.io
    fsType: ext4
    volumeAttributes:
      numberOfReplicates: "1"
      staleReplicaTimeout: "2880"
      dataLocality: "strict-local"
    volumeHandle: pvc-authentik-redis # must match volume name from Longhorn
