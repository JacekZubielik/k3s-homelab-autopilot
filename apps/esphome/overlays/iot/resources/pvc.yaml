---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: config-pvc-volume
  namespace: iot-k3s
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: host
  volumeMode: Filesystem
  volumeName: config-pv-volume
