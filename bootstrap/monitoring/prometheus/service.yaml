apiVersion: v1
kind: Service
metadata:
  name: prometheus
  # annotations:
    # metallb.universe.tf/loadBalancerIPs: 192.168.40.181
spec:
  ports:
  - protocol: TCP
    port: 9090
    targetPort: 9090
  selector:
    app: prometheus
  type: ClusterIP